1. Make sure `gradle/verification-keyring.keys` and `gradle/verification-metadata.xml` have CRLF separators after checkout.
2. Run `./updated-verification.sh` on a Linux/MacOS machine.
3. Observe that the line endings have changed to LF.